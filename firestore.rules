rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Public site content: anyone can read (it's what renders the live
    // website), only signed-in users (the admin) can write.
    match /site/{docId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
